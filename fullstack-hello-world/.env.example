# Backend runtime
PORT=5000
NODE_ENV=development

# Local development database (used with: docker compose --profile local up --build)
DATABASE_URL=postgres://user:password@db:5432/mydatabase

# Production example (set as GitHub secret PROD_DATABASE_URL instead of committing in .env)
# DATABASE_URL=postgres://<prod_user>:<prod_password>@<prod_host>:5432/<prod_db>
